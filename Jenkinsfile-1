pipeline {
  agent {
    docker { image 'ubuntu' }
  }
  stages {
    stage('Test') {
      steps {
        bat 'ubuntu --version'
      }
    }
    
    stage('build'){
      steps{
        bat 'docker build -td ubuntu .'
       }
    }
  }
}
